import { useState } from 'react';
import Link from 'next/link';

type SearchBarProps = {
  changeSearchTerm: () => void;
};

export default function SearchBar({}) {
  <input
    className="w-8 italic rounded full"
    placeholder="Search for Canvases"
    onChange={() => {}}
  ></input>;
}
